import { X } from 'lucide-react';
import { Button } from '../ui/button';
import { generateAnamnesePDF } from '../../utils/generatePDF';

interface AnamneseDetailsProps {
  anamnese: any;
  onClose: () => void;
}

export function AnamneseDetails({ anamnese, onClose }: AnamneseDetailsProps) {
  // 🎨 OBTER CORES TEMÁTICAS DA PROFISSÃO
  const getCoresTema = () => {
    const config = localStorage.getItem('anamneseConfig');
    const templateProfissao = config ? JSON.parse(config).templateProfissao : 'tatuagem';

    const cores: any = {
      tatuagem: {
        gradient: 'from-pink-500 to-purple-500',
        bg50: 'bg-pink-50',
        bg100: 'bg-pink-100',
        bg500: 'bg-pink-500',
        text500: 'text-pink-500',
        text700: 'text-pink-700',
        border200: 'border-pink-200',
        border300: 'border-pink-300',
        border500: 'border-pink-500',
        hover: 'hover:bg-pink-50 hover:border-pink-500',
        focus: 'focus:border-pink-500',
      },
      psicologia: {
        gradient: 'from-blue-500 to-cyan-500',
        bg50: 'bg-blue-50',
        bg100: 'bg-blue-100',
        bg500: 'bg-blue-500',
        text500: 'text-blue-500',
        text700: 'text-blue-700',
        border200: 'border-blue-200',
        border300: 'border-blue-300',
        border500: 'border-blue-500',
        hover: 'hover:bg-blue-50 hover:border-blue-500',
        focus: 'focus:border-blue-500',
      },
      nutricao: {
        gradient: 'from-green-500 to-emerald-500',
        bg50: 'bg-green-50',
        bg100: 'bg-green-100',
        bg500: 'bg-green-500',
        text500: 'text-green-500',
        text700: 'text-green-700',
        border200: 'border-green-200',
        border300: 'border-green-300',
        border500: 'border-green-500',
        hover: 'hover:bg-green-50 hover:border-green-500',
        focus: 'focus:border-green-500',
      },
      fisioterapia: {
        gradient: 'from-orange-500 to-amber-500',
        bg50: 'bg-orange-50',
        bg100: 'bg-orange-100',
        bg500: 'bg-orange-500',
        text500: 'text-orange-500',
        text700: 'text-orange-700',
        border200: 'border-orange-200',
        border300: 'border-orange-300',
        border500: 'border-orange-500',
        hover: 'hover:bg-orange-50 hover:border-orange-500',
        focus: 'focus:border-orange-500',
      },
      estetica: {
        gradient: 'from-purple-500 to-fuchsia-500',
        bg50: 'bg-purple-50',
        bg100: 'bg-purple-100',
        bg500: 'bg-purple-500',
        text500: 'text-purple-500',
        text700: 'text-purple-700',
        border200: 'border-purple-200',
        border300: 'border-purple-300',
        border500: 'border-purple-500',
        hover: 'hover:bg-purple-50 hover:border-purple-500',
        focus: 'focus:border-purple-500',
      },
    };
    return cores[templateProfissao] || cores.tatuagem;
  };

  const coresTema = getCoresTema();

  // Detectar profissão atual
  const config = localStorage.getItem('anamneseConfig');
  const profissao = config ? JSON.parse(config).templateProfissao : 'tatuagem';

  return (
    <div className="fixed inset-0 bg-black/50 backdrop-blur-sm z-50 flex items-center justify-center p-4 animate-fadeIn overflow-y-auto">
      <div className="bg-white rounded-2xl shadow-2xl w-full max-w-4xl max-h-[90vh] overflow-hidden my-8">
        {/* Header */}
        <div className={`bg-gradient-to-r ${coresTema.gradient} p-6 relative`}>
          <button
            onClick={onClose}
            className="absolute top-4 right-4 w-10 h-10 bg-white/20 hover:bg-white/30 rounded-full flex items-center justify-center transition-colors"
          >
            <X className="h-5 w-5 text-white" />
          </button>
          <div className="text-white">
            <h2 className="text-3xl font-bold mb-2">{anamnese.clienteNome}</h2>
            <div className="flex items-center gap-4 text-sm opacity-90">
              <span>📅 {anamnese.data}</span>
              <span>•</span>
              <span>
                {anamnese.preenchidoPor === 'profissional' ? '👩‍⚕️ Preenchida pela Profissional' : '📱 Preenchida pelo Cliente'}
              </span>
              <span>•</span>
              <span>v{anamnese.versao}</span>
            </div>
          </div>
        </div>

        {/* Content */}
        <div className="p-6 overflow-y-auto max-h-[calc(90vh-200px)]">
          {/* Status */}
          <div className="mb-6 flex items-center gap-3">
            <span className="text-sm font-medium text-gray-600">Status:</span>
            {anamnese.status === 'concluida' ? (
              <span className="px-4 py-2 bg-green-100 text-green-700 text-sm font-semibold rounded-full">
                ✓ Concluída
              </span>
            ) : anamnese.status === 'pendente' ? (
              <span className="px-4 py-2 bg-yellow-100 text-yellow-700 text-sm font-semibold rounded-full">
                ⏳ Pendente - Aguardando Cliente
              </span>
            ) : (
              <span className="px-4 py-2 bg-red-100 text-red-700 text-sm font-semibold rounded-full">
                ✕ Expirada
              </span>
            )}
          </div>

          {/* Se pendente, mostrar link */}
          {anamnese.status === 'pendente' && anamnese.linkEnviado && (
            <div className="mb-6 p-4 bg-yellow-50 border-2 border-yellow-200 rounded-xl">
              <h3 className="text-sm font-semibold text-yellow-800 mb-2">Link enviado ao cliente:</h3>
              <code className="text-sm text-yellow-700 bg-white px-3 py-2 rounded block break-all">
                {anamnese.linkEnviado}
              </code>
              <p className="text-xs text-yellow-600 mt-2">
                Aguardando o cliente preencher o formulário
              </p>
            </div>
          )}

          {/* Dados do Quiz (REAIS do localStorage) */}
          {anamnese.status === 'concluida' && anamnese.dadosCompletos && (
            <div className="space-y-6">
              {/* ========== SEÇÃO FIXA 1: DADOS PESSOAIS ========== */}
              <div className="border-2 border-gray-200 rounded-xl p-5">
                <div className="flex items-center gap-3 mb-4">
                  <div className={`w-10 h-10 ${coresTema.bg100} rounded-full flex items-center justify-center`}>
                    <span className="text-xl">📄</span>
                  </div>
                  <h3 className="text-xl font-bold text-gray-900">Dados Pessoais</h3>
                </div>
                <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
                  <div>
                    <label className="text-sm font-medium text-gray-600">Nome Completo</label>
                    <p className="text-gray-900 font-medium">{anamnese.dadosCompletos.nomeCompleto || 'Não informado'}</p>
                  </div>
                  <div>
                    <label className="text-sm font-medium text-gray-600">Data de Nascimento</label>
                    <p className="text-gray-900 font-medium">
                      {anamnese.dadosCompletos.dataNascimento
                        ? new Date(anamnese.dadosCompletos.dataNascimento).toLocaleDateString('pt-BR')
                        : 'Não informado'}
                    </p>
                  </div>
                  <div>
                    <label className="text-sm font-medium text-gray-600">CPF</label>
                    <p className="text-gray-900 font-medium">{anamnese.dadosCompletos.cpf || 'Não informado'}</p>
                  </div>
                  <div>
                    <label className="text-sm font-medium text-gray-600">Telefone</label>
                    <p className="text-gray-900 font-medium">{anamnese.dadosCompletos.telefone || 'Não informado'}</p>
                  </div>
                  <div>
                    <label className="text-sm font-medium text-gray-600">E-mail</label>
                    <p className="text-gray-900 font-medium">{anamnese.dadosCompletos.email || 'Não informado'}</p>
                  </div>
                  <div className="md:col-span-2">
                    <label className="text-sm font-medium text-gray-600">Endereço Completo</label>
                    {anamnese.dadosCompletos.cep ? (
                      <div className="space-y-1">
                        <p className="text-gray-900 font-medium">
                          {anamnese.dadosCompletos.rua}, {anamnese.dadosCompletos.numero}
                          {anamnese.dadosCompletos.complemento && ` - ${anamnese.dadosCompletos.complemento}`}
                        </p>
                        <p className="text-gray-600 text-sm">
                          {anamnese.dadosCompletos.bairro} - {anamnese.dadosCompletos.cidade}/{anamnese.dadosCompletos.estado}
                        </p>
                        <p className="text-gray-500 text-xs">CEP: {anamnese.dadosCompletos.cep}</p>
                      </div>
                    ) : (
                      <p className="text-gray-900 font-medium">{anamnese.dadosCompletos.endereco || 'Não informado'}</p>
                    )}
                  </div>
                </div>
              </div>

              {/* ========== SEÇÃO DINÂMICA: PERGUNTAS CUSTOMIZADAS ========== */}
              {anamnese.dadosCompletos.perguntasSnapshot &&
               anamnese.dadosCompletos.perguntasSnapshot.length > 0 &&
               anamnese.dadosCompletos.respostasCustomizadas && (
                <div className="border-2 border-gray-200 rounded-xl p-5">
                  <div className="flex items-center gap-3 mb-4">
                    <div className={`w-10 h-10 ${coresTema.bg100} rounded-full flex items-center justify-center`}>
                      <span className="text-xl">🎯</span>
                    </div>
                    <h3 className="text-xl font-bold text-gray-900">Perguntas da Anamnese</h3>
                  </div>

                  <div className="space-y-4">
                    {anamnese.dadosCompletos.perguntasSnapshot.map((pergunta: any, index: number) => {
                      const resposta = anamnese.dadosCompletos.respostasCustomizadas[pergunta.id];

                      return (
                        <div key={pergunta.id} className="pb-4 border-b border-gray-100 last:border-b-0 last:pb-0">
                          {/* Pergunta */}
                          <div className="flex items-start gap-2 mb-2">
                            <span className={`${coresTema.text500} font-bold text-sm`}>#{index + 1}</span>
                            <label className="text-sm font-semibold text-gray-900">
                              {pergunta.titulo}
                              {pergunta.obrigatoria && <span className={`ml-1 ${coresTema.text500}`}>*</span>}
                            </label>
                          </div>

                          {/* Resposta */}
                          <div className={`ml-6 pl-3 border-l-2 ${coresTema.border200}`}>
                            {pergunta.tipo === 'simNao' ? (
                              <p className={`font-medium ${resposta === true || resposta === 'true' ? 'text-green-600' : 'text-gray-600'}`}>
                                {resposta === true || resposta === 'true' ? '✅ Sim' : '❌ Não'}
                              </p>
                            ) : pergunta.tipo === 'multiplaEscolha' ? (
                              <p className="text-gray-900">
                                {Array.isArray(resposta)
                                  ? resposta.join(', ')
                                  : resposta || 'Não respondido'}
                              </p>
                            ) : (
                              <p className="text-gray-900 whitespace-pre-wrap">
                                {resposta || 'Não respondido'}
                              </p>
                            )}
                          </div>
                        </div>
                      );
                    })}
                  </div>
                </div>
              )}

              {/* ========== AVISO: ANAMNESES ANTIGAS ========== */}
              {(!anamnese.dadosCompletos.perguntasSnapshot || anamnese.dadosCompletos.perguntasSnapshot.length === 0) && (
                <div className="border-2 border-yellow-200 bg-yellow-50 rounded-xl p-5">
                  <div className="flex items-center gap-3 mb-2">
                    <span className="text-2xl">⚠️</span>
                    <h3 className="text-lg font-bold text-yellow-900">Anamnese Antiga</h3>
                  </div>
                  <p className="text-sm text-yellow-800">
                    Esta anamnese foi criada antes da atualização do sistema de templates customizáveis.
                    As perguntas específicas não estão disponíveis neste formato.
                  </p>
                </div>
              )}

              {/* Seção 3: Saúde Geral (APENAS TATUAGEM) */}
              {profissao === 'tatuagem' && (
              <div className="border-2 border-gray-200 rounded-xl p-5">
                <div className="flex items-center gap-3 mb-4">
                  <div className="w-10 h-10 bg-red-100 rounded-full flex items-center justify-center">
                    <span className="text-xl">❤️</span>
                  </div>
                  <h3 className="text-xl font-bold text-gray-900">Saúde Geral</h3>
                </div>
                <div className="space-y-4">
                  <div>
                    <label className="text-sm font-medium text-gray-600">Doenças ou condições de saúde</label>
                    <p className="text-gray-900">{anamnese.dadosCompletos.doencas || 'Nenhuma informação'}</p>
                  </div>
                  <div>
                    <label className="text-sm font-medium text-gray-600">Medicamentos em uso</label>
                    <p className="text-gray-900">{anamnese.dadosCompletos.medicamentos || 'Nenhuma informação'}</p>
                  </div>
                </div>
              </div>
              )}

              {/* Seção 4: Alergias (APENAS TATUAGEM) */}
              {profissao === 'tatuagem' && (
              <div className="border-2 border-gray-200 rounded-xl p-5">
                <div className="flex items-center gap-3 mb-4">
                  <div className="w-10 h-10 bg-orange-100 rounded-full flex items-center justify-center">
                    <span className="text-xl">⚠️</span>
                  </div>
                  <h3 className="text-xl font-bold text-gray-900">Alergias</h3>
                </div>
                <div className="space-y-3">
                  <div>
                    <label className="text-sm font-medium text-gray-600">Possui alergias?</label>
                    <p className="text-gray-900 font-medium">
                      {anamnese.dadosCompletos.temAlergias ? '✅ Sim' : '❌ Não'}
                    </p>
                  </div>
                  {anamnese.dadosCompletos.temAlergias && anamnese.dadosCompletos.alergias && (
                    <div>
                      <label className="text-sm font-medium text-gray-600">Quais alergias?</label>
                      <p className="text-gray-900">{anamnese.dadosCompletos.alergias}</p>
                    </div>
                  )}
                </div>
              </div>
              )}

              {/* Seção 5: Condições de Pele (APENAS TATUAGEM) */}
              {profissao === 'tatuagem' && (
              <div className="border-2 border-gray-200 rounded-xl p-5">
                <div className="flex items-center gap-3 mb-4">
                  <div className="w-10 h-10 bg-purple-100 rounded-full flex items-center justify-center">
                    <span className="text-xl">✨</span>
                  </div>
                  <h3 className="text-xl font-bold text-gray-900">Condições de Pele</h3>
                </div>
                <div>
                  <label className="text-sm font-medium text-gray-600">Condições específicas</label>
                  <p className="text-gray-900">{anamnese.dadosCompletos.condicoesPele || 'Nenhuma informação'}</p>
                </div>
              </div>
              )}

              {/* Seção 6: Histórico de Tatuagens (APENAS TATUAGEM) */}
              {profissao === 'tatuagem' && (
              <div className="border-2 border-gray-200 rounded-xl p-5">
                <div className="flex items-center gap-3 mb-4">
                  <div className="w-10 h-10 bg-indigo-100 rounded-full flex items-center justify-center">
                    <span className="text-xl">🎨</span>
                  </div>
                  <h3 className="text-xl font-bold text-gray-900">Histórico de Tatuagens</h3>
                </div>
                <div className="space-y-4">
                  <div>
                    <label className="text-sm font-medium text-gray-600">Já fez tatuagem antes?</label>
                    <p className="text-gray-900 font-medium">
                      {anamnese.dadosCompletos.temTatuagem ? '✅ Sim' : '❌ Não'}
                    </p>
                  </div>
                  {anamnese.dadosCompletos.temTatuagem && anamnese.dadosCompletos.historicoTatuagens && (
                    <div>
                      <label className="text-sm font-medium text-gray-600">Histórico</label>
                      <p className="text-gray-900">{anamnese.dadosCompletos.historicoTatuagens}</p>
                    </div>
                  )}
                </div>
              </div>
              )}

              {/* Seção 7: Nova Tatuagem (APENAS TATUAGEM) */}
              {profissao === 'tatuagem' && (
              <div className="border-2 border-gray-200 rounded-xl p-5">
                <div className="flex items-center gap-3 mb-4">
                  <div className={`w-10 h-10 ${coresTema.bg100} rounded-full flex items-center justify-center`}>
                    <span className="text-xl">🖼️</span>
                  </div>
                  <h3 className="text-xl font-bold text-gray-900">Nova Tatuagem</h3>
                </div>
                <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
                  <div>
                    <label className="text-sm font-medium text-gray-600">Local</label>
                    <p className="text-gray-900 font-medium">{anamnese.dadosCompletos.localTatuagem || 'Não informado'}</p>
                  </div>
                  <div>
                    <label className="text-sm font-medium text-gray-600">Tamanho</label>
                    <p className="text-gray-900 font-medium">{anamnese.dadosCompletos.tamanhoTatuagem || 'Não informado'}</p>
                  </div>
                  <div className="md:col-span-2">
                    <label className="text-sm font-medium text-gray-600">Estilo</label>
                    <p className="text-gray-900 font-medium">{anamnese.dadosCompletos.estiloTatuagem || 'Não informado'}</p>
                  </div>
                </div>
              </div>
              )}

              {/* ========== SEÇÕES DE NUTRIÇÃO (APENAS NUTRIÇÃO) ========== */}
              {/* Objetivo e Dados Corporais */}
              {profissao === 'nutricao' && (anamnese.dadosCompletos.objetivoNutricional || anamnese.dadosCompletos.pesoAtual || anamnese.dadosCompletos.altura) && (
                <div className="border-2 border-gray-200 rounded-xl p-5">
                  <div className="flex items-center gap-3 mb-4">
                    <div className={`w-10 h-10 ${coresTema.bg100} rounded-full flex items-center justify-center`}>
                      <span className="text-xl">🎯</span>
                    </div>
                    <h3 className="text-xl font-bold text-gray-900">Objetivo e Dados Corporais</h3>
                  </div>
                  <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
                    {anamnese.dadosCompletos.objetivoNutricional && (
                      <div className="md:col-span-2">
                        <label className="text-sm font-medium text-gray-600">Objetivo principal</label>
                        <p className="text-gray-900 font-medium">{anamnese.dadosCompletos.objetivoNutricional}</p>
                      </div>
                    )}
                    {anamnese.dadosCompletos.pesoAtual && (
                      <div>
                        <label className="text-sm font-medium text-gray-600">Peso atual</label>
                        <p className="text-gray-900 font-medium">{anamnese.dadosCompletos.pesoAtual} kg</p>
                      </div>
                    )}
                    {anamnese.dadosCompletos.altura && (
                      <div>
                        <label className="text-sm font-medium text-gray-600">Altura</label>
                        <p className="text-gray-900 font-medium">{anamnese.dadosCompletos.altura} cm</p>
                      </div>
                    )}
                    {anamnese.dadosCompletos.imc && (
                      <div>
                        <label className="text-sm font-medium text-gray-600">IMC</label>
                        <p className={`font-bold ${coresTema.text500}`}>{anamnese.dadosCompletos.imc}</p>
                      </div>
                    )}
                    {anamnese.dadosCompletos.historicoEmagrecimento && (
                      <div className="md:col-span-2">
                        <label className="text-sm font-medium text-gray-600">Histórico de tentativas</label>
                        <p className="text-gray-900">{anamnese.dadosCompletos.historicoEmagrecimento}</p>
                      </div>
                    )}
                  </div>
                </div>
              )}

              {/* Hábitos Alimentares */}
              {profissao === 'nutricao' && (anamnese.dadosCompletos.quantasRefeicoes || anamnese.dadosCompletos.alimentosGosta) && (
                <div className="border-2 border-gray-200 rounded-xl p-5">
                  <div className="flex items-center gap-3 mb-4">
                    <div className={`w-10 h-10 ${coresTema.bg100} rounded-full flex items-center justify-center`}>
                      <span className="text-xl">🍽️</span>
                    </div>
                    <h3 className="text-xl font-bold text-gray-900">Hábitos Alimentares</h3>
                  </div>
                  <div className="space-y-4">
                    {anamnese.dadosCompletos.quantasRefeicoes && (
                      <div>
                        <label className="text-sm font-medium text-gray-600">Refeições por dia</label>
                        <p className="text-gray-900 font-medium">{anamnese.dadosCompletos.quantasRefeicoes}</p>
                      </div>
                    )}
                    {anamnese.dadosCompletos.alimentosGosta && (
                      <div>
                        <label className="text-sm font-medium text-gray-600">Alimentos que gosta</label>
                        <p className="text-gray-900">{anamnese.dadosCompletos.alimentosGosta}</p>
                      </div>
                    )}
                    {anamnese.dadosCompletos.alimentosEvita && (
                      <div>
                        <label className="text-sm font-medium text-gray-600">Alimentos que evita</label>
                        <p className="text-gray-900">{anamnese.dadosCompletos.alimentosEvita}</p>
                      </div>
                    )}
                    {anamnese.dadosCompletos.restricaoAlimentar && (
                      <div>
                        <label className="text-sm font-medium text-gray-600">Restrição alimentar</label>
                        <p className="text-gray-900 font-medium">{anamnese.dadosCompletos.restricaoAlimentar}</p>
                      </div>
                    )}
                    {anamnese.dadosCompletos.intoleranciaAlergia && (
                      <div>
                        <label className="text-sm font-medium text-gray-600">Intolerâncias/Alergias</label>
                        <p className="text-gray-900">{anamnese.dadosCompletos.intoleranciaAlergia}</p>
                      </div>
                    )}
                    {anamnese.dadosCompletos.litrosAgua && (
                      <div>
                        <label className="text-sm font-medium text-gray-600">Consumo de água</label>
                        <p className="text-gray-900 font-medium">{anamnese.dadosCompletos.litrosAgua}</p>
                      </div>
                    )}
                  </div>
                </div>
              )}

              {/* Estilo de Vida */}
              {profissao === 'nutricao' && (anamnese.dadosCompletos.atividadeFisica || anamnese.dadosCompletos.qualidadeSono) && (
                <div className="border-2 border-gray-200 rounded-xl p-5">
                  <div className="flex items-center gap-3 mb-4">
                    <div className={`w-10 h-10 ${coresTema.bg100} rounded-full flex items-center justify-center`}>
                      <span className="text-xl">🏃</span>
                    </div>
                    <h3 className="text-xl font-bold text-gray-900">Estilo de Vida</h3>
                  </div>
                  <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
                    {anamnese.dadosCompletos.atividadeFisica && (
                      <div>
                        <label className="text-sm font-medium text-gray-600">Atividade física</label>
                        <p className="text-gray-900 font-medium">{anamnese.dadosCompletos.atividadeFisica}</p>
                      </div>
                    )}
                    {anamnese.dadosCompletos.qualidadeSono && (
                      <div>
                        <label className="text-sm font-medium text-gray-600">Qualidade do sono</label>
                        <p className="text-gray-900 font-medium">{anamnese.dadosCompletos.qualidadeSono}</p>
                      </div>
                    )}
                    {anamnese.dadosCompletos.horasSono && (
                      <div>
                        <label className="text-sm font-medium text-gray-600">Horas de sono</label>
                        <p className="text-gray-900 font-medium">{anamnese.dadosCompletos.horasSono}</p>
                      </div>
                    )}
                    {anamnese.dadosCompletos.funcionamentoIntestinal && (
                      <div>
                        <label className="text-sm font-medium text-gray-600">Funcionamento intestinal</label>
                        <p className="text-gray-900 font-medium">{anamnese.dadosCompletos.funcionamentoIntestinal}</p>
                      </div>
                    )}
                    {anamnese.dadosCompletos.consumoAlcool && (
                      <div>
                        <label className="text-sm font-medium text-gray-600">Consumo de álcool</label>
                        <p className="text-gray-900 font-medium">{anamnese.dadosCompletos.consumoAlcool}</p>
                      </div>
                    )}
                  </div>
                </div>
              )}

              {/* Histórico de Saúde (Nutrição) */}
              {profissao === 'nutricao' && (anamnese.dadosCompletos.doencasCondicoes || anamnese.dadosCompletos.usoSuplementos) && (
                <div className="border-2 border-gray-200 rounded-xl p-5">
                  <div className="flex items-center gap-3 mb-4">
                    <div className={`w-10 h-10 ${coresTema.bg100} rounded-full flex items-center justify-center`}>
                      <span className="text-xl">🩺</span>
                    </div>
                    <h3 className="text-xl font-bold text-gray-900">Histórico de Saúde</h3>
                  </div>
                  <div className="space-y-4">
                    {anamnese.dadosCompletos.doencasCondicoes && (
                      <div>
                        <label className="text-sm font-medium text-gray-600">Doenças/Condições</label>
                        <p className="text-gray-900">{anamnese.dadosCompletos.doencasCondicoes}</p>
                      </div>
                    )}
                    {anamnese.dadosCompletos.usoSuplementos && (
                      <div>
                        <label className="text-sm font-medium text-gray-600">Suplementos</label>
                        <p className="text-gray-900">{anamnese.dadosCompletos.usoSuplementos}</p>
                      </div>
                    )}
                    {anamnese.dadosCompletos.examesRecentes && (
                      <div>
                        <label className="text-sm font-medium text-gray-600">Exames recentes</label>
                        <p className="text-gray-900">{anamnese.dadosCompletos.examesRecentes}</p>
                      </div>
                    )}
                  </div>
                </div>
              )}

              {/* Expectativas Nutricionais */}
              {anamnese.dadosCompletos.expectativasNutricao && (
                <div className="border-2 border-gray-200 rounded-xl p-5">
                  <div className="flex items-center gap-3 mb-4">
                    <div className={`w-10 h-10 ${coresTema.bg100} rounded-full flex items-center justify-center`}>
                      <span className="text-xl">💚</span>
                    </div>
                    <h3 className="text-xl font-bold text-gray-900">Expectativas e Objetivos</h3>
                  </div>
                  <div>
                    <p className="text-gray-900 whitespace-pre-wrap">{anamnese.dadosCompletos.expectativasNutricao}</p>
                  </div>
                </div>
              )}

              {/* ========== SEÇÕES DE PSICOLOGIA (APENAS PSICOLOGIA) ========== */}
              {/* Motivo da Busca e Faixa Etária */}
              {profissao === 'psicologia' && (anamnese.dadosCompletos.faixaEtaria || anamnese.dadosCompletos.motivoBusca || anamnese.dadosCompletos.queixaPrincipal) && (
                <div className="border-2 border-gray-200 rounded-xl p-5">
                  <div className="flex items-center gap-3 mb-4">
                    <div className={`w-10 h-10 ${coresTema.bg100} rounded-full flex items-center justify-center`}>
                      <span className="text-xl">💭</span>
                    </div>
                    <h3 className="text-xl font-bold text-gray-900">Motivo da Busca</h3>
                  </div>
                  <div className="space-y-4">
                    {anamnese.dadosCompletos.faixaEtaria && (
                      <div>
                        <label className="text-sm font-medium text-gray-600">Faixa Etária</label>
                        <p className="text-gray-900 font-medium">{anamnese.dadosCompletos.faixaEtaria} anos</p>
                      </div>
                    )}
                    {anamnese.dadosCompletos.motivoBusca && (
                      <div>
                        <label className="text-sm font-medium text-gray-600">Motivo principal</label>
                        <p className="text-gray-900 font-medium">{anamnese.dadosCompletos.motivoBusca}</p>
                      </div>
                    )}
                    {anamnese.dadosCompletos.queixaPrincipal && (
                      <div>
                        <label className="text-sm font-medium text-gray-600">Queixa Principal</label>
                        <p className="text-gray-900 whitespace-pre-wrap">{anamnese.dadosCompletos.queixaPrincipal}</p>
                      </div>
                    )}
                    {anamnese.dadosCompletos.sintomas && (
                      <div>
                        <label className="text-sm font-medium text-gray-600">Sintomas</label>
                        <p className="text-gray-900 whitespace-pre-wrap">{anamnese.dadosCompletos.sintomas}</p>
                      </div>
                    )}
                  </div>
                </div>
              )}

              {/* Histórico de Tratamento (Psicologia) */}
              {profissao === 'psicologia' && (anamnese.dadosCompletos.historicoTratamento || anamnese.dadosCompletos.medicamentosUso || anamnese.dadosCompletos.historiaFamiliar) && (
                <div className="border-2 border-gray-200 rounded-xl p-5">
                  <div className="flex items-center gap-3 mb-4">
                    <div className={`w-10 h-10 ${coresTema.bg100} rounded-full flex items-center justify-center`}>
                      <span className="text-xl">📋</span>
                    </div>
                    <h3 className="text-xl font-bold text-gray-900">Histórico de Tratamento</h3>
                  </div>
                  <div className="space-y-4">
                    {anamnese.dadosCompletos.historicoTratamento && (
                      <div>
                        <label className="text-sm font-medium text-gray-600">Tratamentos anteriores</label>
                        <p className="text-gray-900 font-medium">{anamnese.dadosCompletos.historicoTratamento}</p>
                      </div>
                    )}
                    {anamnese.dadosCompletos.medicamentosUso && (
                      <div>
                        <label className="text-sm font-medium text-gray-600">Medicamentos psiquiátricos</label>
                        <p className="text-gray-900 whitespace-pre-wrap">{anamnese.dadosCompletos.medicamentosUso}</p>
                      </div>
                    )}
                    {anamnese.dadosCompletos.historiaFamiliar && (
                      <div>
                        <label className="text-sm font-medium text-gray-600">Histórico familiar</label>
                        <p className="text-gray-900 whitespace-pre-wrap">{anamnese.dadosCompletos.historiaFamiliar}</p>
                      </div>
                    )}
                  </div>
                </div>
              )}

              {/* História de Vida (Psicologia) */}
              {profissao === 'psicologia' && (anamnese.dadosCompletos.eventosSignificativos || anamnese.dadosCompletos.apoioSocial) && (
                <div className="border-2 border-gray-200 rounded-xl p-5">
                  <div className="flex items-center gap-3 mb-4">
                    <div className={`w-10 h-10 ${coresTema.bg100} rounded-full flex items-center justify-center`}>
                      <span className="text-xl">🌟</span>
                    </div>
                    <h3 className="text-xl font-bold text-gray-900">História de Vida</h3>
                  </div>
                  <div className="space-y-4">
                    {anamnese.dadosCompletos.eventosSignificativos && (
                      <div>
                        <label className="text-sm font-medium text-gray-600">Eventos significativos</label>
                        <p className="text-gray-900 whitespace-pre-wrap">{anamnese.dadosCompletos.eventosSignificativos}</p>
                      </div>
                    )}
                    {anamnese.dadosCompletos.apoioSocial && (
                      <div>
                        <label className="text-sm font-medium text-gray-600">Rede de apoio</label>
                        <p className="text-gray-900 whitespace-pre-wrap">{anamnese.dadosCompletos.apoioSocial}</p>
                      </div>
                    )}
                  </div>
                </div>
              )}

              {/* Objetivos e Expectativas (Psicologia) */}
              {profissao === 'psicologia' && (anamnese.dadosCompletos.objetivosTerapia || anamnese.dadosCompletos.expectativasPsicologia) && (
                <div className="border-2 border-gray-200 rounded-xl p-5">
                  <div className="flex items-center gap-3 mb-4">
                    <div className={`w-10 h-10 ${coresTema.bg100} rounded-full flex items-center justify-center`}>
                      <span className="text-xl">🎯</span>
                    </div>
                    <h3 className="text-xl font-bold text-gray-900">Objetivos e Expectativas</h3>
                  </div>
                  <div className="space-y-4">
                    {anamnese.dadosCompletos.objetivosTerapia && (
                      <div>
                        <label className="text-sm font-medium text-gray-600">Objetivos da terapia</label>
                        <p className="text-gray-900 whitespace-pre-wrap">{anamnese.dadosCompletos.objetivosTerapia}</p>
                      </div>
                    )}
                    {anamnese.dadosCompletos.expectativasPsicologia && (
                      <div>
                        <label className="text-sm font-medium text-gray-600">Expectativas gerais</label>
                        <p className="text-gray-900 whitespace-pre-wrap">{anamnese.dadosCompletos.expectativasPsicologia}</p>
                      </div>
                    )}
                  </div>
                </div>
              )}

              {/* ========== SEÇÕES DE FISIOTERAPIA (APENAS FISIOTERAPIA) ========== */}
              {/* Motivo da Consulta (Fisioterapia) */}
              {profissao === 'fisioterapia' && (anamnese.dadosCompletos.motivoConsultaFisio || anamnese.dadosCompletos.quandoComecou || anamnese.dadosCompletos.oQueEstavaFazendo) && (
                <div className="border-2 border-gray-200 rounded-xl p-5">
                  <div className="flex items-center gap-3 mb-4">
                    <div className={`w-10 h-10 ${coresTema.bg100} rounded-full flex items-center justify-center`}>
                      <span className="text-xl">💪</span>
                    </div>
                    <h3 className="text-xl font-bold text-gray-900">Motivo da Consulta</h3>
                  </div>
                  <div className="space-y-4">
                    {anamnese.dadosCompletos.motivoConsultaFisio && (
                      <div>
                        <label className="text-sm font-medium text-gray-600">Motivo principal</label>
                        <p className="text-gray-900 font-medium">{anamnese.dadosCompletos.motivoConsultaFisio}</p>
                      </div>
                    )}
                    {anamnese.dadosCompletos.quandoComecou && (
                      <div>
                        <label className="text-sm font-medium text-gray-600">Quando começou</label>
                        <p className="text-gray-900 font-medium">{anamnese.dadosCompletos.quandoComecou}</p>
                      </div>
                    )}
                    {anamnese.dadosCompletos.oQueEstavaFazendo && (
                      <div>
                        <label className="text-sm font-medium text-gray-600">O que estava fazendo</label>
                        <p className="text-gray-900 whitespace-pre-wrap">{anamnese.dadosCompletos.oQueEstavaFazendo}</p>
                      </div>
                    )}
                  </div>
                </div>
              )}

              {/* Dor e Sintomas (Fisioterapia) */}
              {profissao === 'fisioterapia' && (anamnese.dadosCompletos.localizacaoDor || anamnese.dadosCompletos.intensidadeDor) && (
                <div className="border-2 border-gray-200 rounded-xl p-5">
                  <div className="flex items-center gap-3 mb-4">
                    <div className={`w-10 h-10 ${coresTema.bg100} rounded-full flex items-center justify-center`}>
                      <span className="text-xl">⚡</span>
                    </div>
                    <h3 className="text-xl font-bold text-gray-900">Dor e Sintomas</h3>
                  </div>
                  <div className="space-y-4">
                    {anamnese.dadosCompletos.localizacaoDor && (
                      <div>
                        <label className="text-sm font-medium text-gray-600">Localização da dor</label>
                        <p className="text-gray-900 font-medium">{anamnese.dadosCompletos.localizacaoDor}</p>
                      </div>
                    )}
                    {anamnese.dadosCompletos.intensidadeDor && (
                      <div>
                        <label className="text-sm font-medium text-gray-600">Intensidade da dor</label>
                        <p className={`font-bold text-lg ${coresTema.text500}`}>{anamnese.dadosCompletos.intensidadeDor}/10</p>
                      </div>
                    )}
                    {anamnese.dadosCompletos.tipoDor && (
                      <div>
                        <label className="text-sm font-medium text-gray-600">Tipo de dor</label>
                        <p className="text-gray-900 font-medium">{anamnese.dadosCompletos.tipoDor}</p>
                      </div>
                    )}
                    {anamnese.dadosCompletos.oQuePioraDor && (
                      <div>
                        <label className="text-sm font-medium text-gray-600">O que piora a dor</label>
                        <p className="text-gray-900 whitespace-pre-wrap">{anamnese.dadosCompletos.oQuePioraDor}</p>
                      </div>
                    )}
                    {anamnese.dadosCompletos.oQueMelhoraDor && (
                      <div>
                        <label className="text-sm font-medium text-gray-600">O que melhora a dor</label>
                        <p className="text-gray-900 whitespace-pre-wrap">{anamnese.dadosCompletos.oQueMelhoraDor}</p>
                      </div>
                    )}
                  </div>
                </div>
              )}

              {/* Histórico Médico (Fisioterapia) */}
              {profissao === 'fisioterapia' && (anamnese.dadosCompletos.jafezFisioterapia || anamnese.dadosCompletos.historicoCirurgias || anamnese.dadosCompletos.medicamentosDor || anamnese.dadosCompletos.doencasCronicas) && (
                <div className="border-2 border-gray-200 rounded-xl p-5">
                  <div className="flex items-center gap-3 mb-4">
                    <div className={`w-10 h-10 ${coresTema.bg100} rounded-full flex items-center justify-center`}>
                      <span className="text-xl">📋</span>
                    </div>
                    <h3 className="text-xl font-bold text-gray-900">Histórico Médico</h3>
                  </div>
                  <div className="space-y-4">
                    {anamnese.dadosCompletos.jafezFisioterapia && (
                      <div>
                        <label className="text-sm font-medium text-gray-600">Já fez fisioterapia antes?</label>
                        <p className="text-gray-900 font-medium">{anamnese.dadosCompletos.jafezFisioterapia}</p>
                      </div>
                    )}
                    {anamnese.dadosCompletos.historicoCirurgias && (
                      <div>
                        <label className="text-sm font-medium text-gray-600">Histórico de cirurgias</label>
                        <p className="text-gray-900 whitespace-pre-wrap">{anamnese.dadosCompletos.historicoCirurgias}</p>
                      </div>
                    )}
                    {anamnese.dadosCompletos.medicamentosDor && (
                      <div>
                        <label className="text-sm font-medium text-gray-600">Medicamentos para dor</label>
                        <p className="text-gray-900 whitespace-pre-wrap">{anamnese.dadosCompletos.medicamentosDor}</p>
                      </div>
                    )}
                    {anamnese.dadosCompletos.doencasCronicas && (
                      <div>
                        <label className="text-sm font-medium text-gray-600">Doenças crônicas</label>
                        <p className="text-gray-900 whitespace-pre-wrap">{anamnese.dadosCompletos.doencasCronicas}</p>
                      </div>
                    )}
                  </div>
                </div>
              )}

              {/* Atividades e Mobilidade (Fisioterapia) */}
              {profissao === 'fisioterapia' && (anamnese.dadosCompletos.atividadeFisicaFisio || anamnese.dadosCompletos.limitacoesMovimento || anamnese.dadosCompletos.usoOrteses || anamnese.dadosCompletos.profissaoAtividades) && (
                <div className="border-2 border-gray-200 rounded-xl p-5">
                  <div className="flex items-center gap-3 mb-4">
                    <div className={`w-10 h-10 ${coresTema.bg100} rounded-full flex items-center justify-center`}>
                      <span className="text-xl">🏃</span>
                    </div>
                    <h3 className="text-xl font-bold text-gray-900">Atividades e Mobilidade</h3>
                  </div>
                  <div className="space-y-4">
                    {anamnese.dadosCompletos.atividadeFisicaFisio && (
                      <div>
                        <label className="text-sm font-medium text-gray-600">Atividade física</label>
                        <p className="text-gray-900 font-medium">{anamnese.dadosCompletos.atividadeFisicaFisio}</p>
                      </div>
                    )}
                    {anamnese.dadosCompletos.limitacoesMovimento && (
                      <div>
                        <label className="text-sm font-medium text-gray-600">Limitações de movimento</label>
                        <p className="text-gray-900 whitespace-pre-wrap">{anamnese.dadosCompletos.limitacoesMovimento}</p>
                      </div>
                    )}
                    {anamnese.dadosCompletos.usoOrteses && (
                      <div>
                        <label className="text-sm font-medium text-gray-600">Uso de órteses/próteses</label>
                        <p className="text-gray-900 whitespace-pre-wrap">{anamnese.dadosCompletos.usoOrteses}</p>
                      </div>
                    )}
                    {anamnese.dadosCompletos.profissaoAtividades && (
                      <div>
                        <label className="text-sm font-medium text-gray-600">Profissão e atividades diárias</label>
                        <p className="text-gray-900 whitespace-pre-wrap">{anamnese.dadosCompletos.profissaoAtividades}</p>
                      </div>
                    )}
                  </div>
                </div>
              )}

              {/* Objetivos (Fisioterapia) */}
              {profissao === 'fisioterapia' && anamnese.dadosCompletos.objetivosFisioterapia && (
                <div className="border-2 border-gray-200 rounded-xl p-5">
                  <div className="flex items-center gap-3 mb-4">
                    <div className={`w-10 h-10 ${coresTema.bg100} rounded-full flex items-center justify-center`}>
                      <span className="text-xl">🎯</span>
                    </div>
                    <h3 className="text-xl font-bold text-gray-900">Objetivos</h3>
                  </div>
                  <div>
                    <label className="text-sm font-medium text-gray-600">O que espera alcançar</label>
                    <p className="text-gray-900 whitespace-pre-wrap">{anamnese.dadosCompletos.objetivosFisioterapia}</p>
                  </div>
                </div>
              )}

              {/* Seção: Data da Anamnese */}
              {anamnese.dadosCompletos.dataAnamnese && (
                <div className="border-2 border-gray-200 rounded-xl p-5">
                  <div className="flex items-center gap-3 mb-4">
                    <div className={`w-10 h-10 ${coresTema.bg100} rounded-full flex items-center justify-center`}>
                      <span className="text-xl">📅</span>
                    </div>
                    <h3 className="text-xl font-bold text-gray-900">Data da Anamnese</h3>
                  </div>
                  <div>
                    <label className="text-sm font-medium text-gray-600">Data de realização</label>
                    <p className="text-gray-900 font-medium text-lg">
                      {new Date(anamnese.dadosCompletos.dataAnamnese + 'T00:00:00').toLocaleDateString('pt-BR', {
                        day: '2-digit',
                        month: 'long',
                        year: 'numeric'
                      })}
                    </p>
                  </div>
                </div>
              )}

              {/* Seção: Termo e Assinatura */}
              <div className="border-2 border-gray-200 rounded-xl p-5">
                <div className="flex items-center gap-3 mb-4">
                  <div className="w-10 h-10 bg-emerald-100 rounded-full flex items-center justify-center">
                    <span className="text-xl">✅</span>
                  </div>
                  <h3 className="text-xl font-bold text-gray-900">Termo de Compromisso</h3>
                </div>
                <div className="space-y-4">
                  <div>
                    <label className="text-sm font-medium text-gray-600">Termo aceito?</label>
                    <p className={`font-semibold ${anamnese.dadosCompletos.aceitaTermo ? 'text-emerald-600' : 'text-red-600'}`}>
                      {anamnese.dadosCompletos.aceitaTermo ? '✓ Sim, aceito' : '✗ Não aceito'}
                    </p>
                  </div>
                  <div>
                    <label className="text-sm font-medium text-gray-600">Assinatura Digital</label>
                    <p className="text-gray-900 font-signature text-2xl" style={{ fontFamily: 'cursive' }}>
                      {anamnese.dadosCompletos.assinatura || anamnese.clienteNome}
                    </p>
                  </div>
                </div>
              </div>
            </div>
          )}

          {/* Se pendente, não mostra dados */}
          {anamnese.status === 'pendente' && (
            <div className="text-center py-12">
              <div className="w-20 h-20 bg-yellow-100 rounded-full flex items-center justify-center mx-auto mb-4">
                <span className="text-4xl">⏳</span>
              </div>
              <h3 className="text-xl font-bold text-gray-900 mb-2">Aguardando Preenchimento</h3>
              <p className="text-gray-600">
                O cliente ainda não preencheu a anamnese. Os dados aparecerão aqui assim que ele concluir.
              </p>
            </div>
          )}
        </div>

        {/* Footer */}
        <div className="border-t border-gray-200 p-4 flex justify-end gap-3 bg-gray-50">
          <Button variant="outline" onClick={onClose}>
            Fechar
          </Button>
          {anamnese.status === 'concluida' && (
            <Button
              className={`bg-gradient-to-r ${coresTema.gradient}`}
              onClick={() => {
                generateAnamnesePDF(anamnese);
                onClose();
              }}
            >
              📄 Gerar PDF
            </Button>
          )}
        </div>
      </div>

      <style>{`
        @keyframes fadeIn {
          from { opacity: 0; transform: scale(0.95); }
          to { opacity: 1; transform: scale(1); }
        }
        .animate-fadeIn {
          animation: fadeIn 0.3s ease-out;
        }
      `}</style>
    </div>
  );
}
